apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ globex_ui_name }}
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: {{ globex_ui_name }}
    weight: 100
  wildcardPolicy: None
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge

