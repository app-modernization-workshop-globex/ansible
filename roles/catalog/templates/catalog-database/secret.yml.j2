kind: Secret
apiVersion: v1
metadata:
  name: {{ catalog_db_name }}
  labels:
    app.kubernetes.io/name: {{ catalog_db_name }}
    app.kubernetes.io/part-of: {{ catalog_app_name }}
stringData:
  database-user: {{ catalog_db_user }}
  database-password: {{ catalog_db_password }}
  database-name: {{ catalog_db_database }}
type: Opaque